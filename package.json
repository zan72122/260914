{
  "name": "nichijou-small-incidents",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --port 4173",
    "shots": "node scripts/shots.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}

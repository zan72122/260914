{
  "name": "halloween-night",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:test": "TESTABLE=1 vite build",
    "preview": "vite preview",
    "test": "npm run build:test && node tests/scenario.spec.mjs",
    "test:full": "npm run build:test && node tests/walkthrough.spec.mjs"
  },
  "dependencies": {
    "three": "^0.186.0",
    "vite": "^8.3.0"
  }
}

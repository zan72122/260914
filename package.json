{
  "name": "tune-train",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/three": "^0.186.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "5.0.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}

# つきとしお

Sizeable 風のサイズ変化ジオラマパズル(プロトタイプ)。iPhone / iPad の Safari 向け。
文字を一切使わず、世界そのものが次の操作を誘う設計。詳細は `docs/01.md`。

- 触れるものは常にかすかに脈動している。触って上下にドラッグすると大きく/小さくなる。
- 月を小さくすると潮が引き、海底に隠れた柱が現れる。柱をタップすると祝福。
- 空いた場所を左右にドラッグするとジオラマが回る。

```
npm install
npm run dev
npm run build   # dist/ を生成
```

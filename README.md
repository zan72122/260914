# 星を起こす（Hoshi o Okosu）

4歳向け・文字なし・一本指だけで遊ぶ、スターサファイア研磨あそび。

## 起動

```sh
npm install
npm run dev      # 開発サーバ
npm run build    # dist/ を生成（静的ホスティング可）
npm run preview  # build 後の確認
npm run playtest # Playwright で縦横 1 周自動試遊 + スクリーンショット
```

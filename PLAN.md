# Goo Garden — World of Goo 風・言葉なしパズル(実装プラン)

対象: iPhone / iPad の Safari(縦・横どちらでも)。
プレイヤー像: 4歳女児。**文字を一切読ませない**(Wordless interaction 厳守)。
本ファイルは承認前のプランであり、実装はまだ行っていない。

---

## 1. 確定した方針(Q&A 結果)

| 項目 | 決定 |
|---|---|
| 技術 | 素の HTML + Canvas 2D + 自作バネ質点物理。依存ゼロ、ビルド不要 |
| ステージ目的 | 原作同様「吸い込みパイプ」まで構造を届かせ、残りの Goo を吸わせる |
| 失敗 | なし。落ちた Goo は消えず、転がって拾える場所へ歩いて戻る |
| 規模 | 3 ステージ、Goo 1 種(くっつく黒Goo相当。見た目は丸くて目がある) |
| 遷移 | クリア → 祝福 → カメラがパイプの先へ流れて次ステージ。最終後は先頭へ。ボタン無し |
| 音 | Web Audio でコード合成(効果音 + ループBGM)。音声ファイル無し |
| 公開 | GitHub Pages 想定。ルート `index.html` + `js/` 分割 + `manifest.webmanifest` |

---

## 2. Wordless 設計原則 → 具体策

「説明の代わりに世界そのものをUIにする」を、以下の 6 原則で機械的に適用する。

| 原則 | 実装 |
|---|---|
| 知覚されるアフォーダンス | Goo は常に「ぷるぷる」呼吸して柔らかさを示す。触れると凹む。「つまめるもの」に見える |
| 顕著なシグニファイア | 指で Goo を持ち上げると、置ける位置に **点線の仮ストランド(バネ)** が伸びて追従する。緑に光れば置ける、届かなければ線が出ない |
| 慣れ親しんだ物理因果 | 重力・揺れ・崩れ。積みすぎると傾き、きしみ音がする(音と傾きで警告、文字なし) |
| 自然なマッピング | 指の位置 = Goo の位置。ドラッグ以外の操作を作らない |
| 知覚的な不完全性 | パイプの口は画面内に必ず見え、**口をぱくぱく開けて待っている**。構造とパイプの間に「隙間」があること自体が課題を示す |
| 即時の視覚フィードバック | 置いた瞬間に「ぷにゅ」音+波紋、Goo たちが新しい足場を登り始める |

### 禁止事項(実装時にレビューで確認)
- 文字・数字・アイコン付きボタンを **一切** 描画しない(HUD なし)。
- 「タイムアウト」「残機」「スコア」を持たない。
- モーダル・ダイアログ・確認画面を出さない。
- 説明ヒント(矢印テキスト等)を出さない。手本は **Goo 自身の動き** で示す(下記 3-1)。

---

## 3. ゲーム仕様

### 3-1. コアループ
1. 画面下部の「足場」に Goo の群れがうごめいている(10〜16 匹)。
2. 足場の端にはすでに小さな **初期構造(三角形 1 つ)** が置いてあり、既にくっついた Goo がその上を歩き回っている。→ 「これは積めるもの」だと見て分かる。
3. **最初の 5 秒、誰も触らなければ**、Goo が 1 匹自分で跳ねて構造にくっつく **お手本デモ** を 1 回だけ行う(その後は行わない)。
4. 指で自由な Goo をつまむ → 近くの 2 点以上の固定 Goo に仮ストランドが伸びる → 離すと接続され構造が伸びる。
5. 構造がパイプの吸い込み範囲に入ると、パイプが「ゴクリ」と口を伸ばして吸引開始。自由な Goo が構造を登って吸われていく。
6. 一定数吸われるとクリア演出 → 自動で次ステージ。

### 3-2. Goo(1 種)
- 半径 ~14px(画面幅に対して相対スケール)。目 2 つ、瞬き、視線は指を追う。
- 状態: `free`(足場を歩く/構造の上を歩く)、`held`(指)、`fixed`(構造の節)。
- 1 節あたり最大接続 3 本。接続距離は半径の 4〜7 倍。
- 落下しても消滅しない。地面(画面外下)で跳ねて足場方向へコロコロ戻り、`free` に復帰。

### 3-3. 物理(自作)
- 質点 + バネ(Hooke + 減衰)。Verlet 積分、固定サブステップ(1/120s)、3 回反復。
- 地面/足場/壁は静的な線分。ステージは矩形 + 傾斜 + 突起で構成。
- 構造の傾き検出で「きしみ」効果音 → 崩壊はする。崩壊は罰ではなく「面白い出来事」として扱い、Goo は転がって戻る。

### 3-4. ステージ(3 面、すべて同じルール)
| # | 見た目 | 教えること | 配置 |
|---|---|---|---|
| 1 | 草原・昼 | 上に積むと届く | 足場の真上、少し高い所にパイプ。まっすぐ積むだけ |
| 2 | 夕焼け・崖 | 横に伸ばして渡る | 足場の右、間に谷。橋を伸ばすとしなる |
| 3 | 夜・星 | 斜めに登る | 足場の左上、途中に小さな岩の突起(寄りかかれる) |

クリア条件: 自由 Goo が **4 匹** 吸われる(常に達成可能な余剰を確保: 総数 ≥ 構造必要数 + 4 + 余裕 4)。

### 3-5. クリア演出と遷移(文字なし)
- パイプが満腹になって膨らみ、口が閉じ、「ぽんっ」と紙吹雪。
- 画面が **パイプの向き** にパンして次の世界へつながる(パイプが導線)。
- 3 面クリア後はステージ 1 に戻り、空の色が少し変わる(周回感)。

### 3-6. 入力(iOS)
- Pointer Events(touch/mouse 両対応)。マルチタッチは **最初の 1 本指のみ有効**、それ以外は無視(誤操作防止)。
- `touch-action: none`、ダブルタップズーム/長押し選択/ピンチ無効化、100vh 問題は `visualViewport` で対応。
- 縦・横どちらでも: ステージ座標は「基準ワールド」を保持し、画面比率に応じて **letterbox ではなく安全領域内に収まるスケール** で描画。パイプと足場は必ず画面内。
- `orientationchange` / `resize` で即再レイアウト、状態は保持。

### 3-7. 音(Web Audio 合成)
- 効果音: つまむ「きゅっ」、置く「ぷにゅ」、接続「ぽん」、きしみ、吸引「ちゅるっ」、クリア「ぱぱーん」。
- BGM: 三和音のオルゴール風ループ(8 小節)。ステージごとに調を変える。
- iOS 制約: 初回タッチ時に `AudioContext.resume()`。無音でも遊べる設計。
- 音量調整 UI は作らない(端末側で)。

### 3-8. 見た目
- ベクター描画のみ(画像アセットなし)。丸・柔らかい・パステル寄りだが「ピンク一色」にはしない。
- 背景は 2 層パララックス。Goo に影。パイプは金属質で口がゴム風。

---

## 4. ファイル構成

```
index.html               … canvas 1 枚 + viewport/PWA meta
manifest.webmanifest     … ホーム画面追加用(アイコンは SVG data URI)
css/style.css            … 全画面・タッチ無効化
js/main.js               … 起動、ループ、リサイズ、ステージ遷移
js/physics.js            … 質点/バネ/衝突(Verlet)
js/goo.js                … Goo の状態機械・歩行・表情
js/structure.js          … 接続グラフ、仮ストランド判定、パイプ到達判定
js/input.js              … Pointer Events → 1 本指ドラッグ
js/levels.js             … 3 ステージのデータ(地形線分・足場・パイプ位置・色)
js/render.js             … 描画(背景、地形、ストランド、Goo、パイプ、演出)
js/audio.js              … Web Audio 合成(SE + BGM)
js/fx.js                 … 紙吹雪・波紋・パン演出
PLAN.md                  … 本ファイル
```

---

## 5. 実装順(承認後)

1. `index.html` / `main.js` / `input.js` — 全画面 canvas、リサイズ、1 本指ドラッグ、iOS 対策。
2. `physics.js` / `structure.js` — バネ構造とドラッグ接続、仮ストランド表示。
3. `goo.js` — 歩行・登る・落ちて戻る・表情。
4. `levels.js` / `render.js` — ステージ 1、パイプ吸引、クリア → 自動遷移。
5. ステージ 2・3、お手本デモ、パララックス。
6. `audio.js` — SE と BGM。
7. `fx.js` — 祝福演出、周回。
8. 検証: Playwright(Chromium)で iPhone 縦/横・iPad 縦/横のビューポートを撮影し、「文字が 1 つも無いこと」「足場とパイプが画面内にあること」を確認。

---

## 6. 承認前の確認事項
- 上記で未合意の細部(Goo の数、クリア必要匹数など)は実装中に調整可とする想定。
- 承認後に実装を開始し、ステップ 4 完了(ステージ 1 が遊べる時点)で一度共有する。
